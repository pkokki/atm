angular.module('atlas.jobManager', [
		'atlas.client',
	])
	/******************************************************************************************************************
	APP CONFIG ********************************************************************************************************
	*******************************************************************************************************************/
	//.config([function () {
	//}])
	/******************************************************************************************************************
	APP RUN ***********************************************************************************************************
	*******************************************************************************************************************/
	//.run(['$rootScope', function ($rootScope) {
	//}])
	/******************************************************************************************************************
	SERVICES **********************************************************************************************************
	*******************************************************************************************************************/
	.provider('jobManager', [function() {
		var jobTemplates = [
			{ name: 'j1', priority: 5, }
		];
		var config = function(jobTemplates) {
			jobTemplates = jobTemplates;
		};
		var serviceFactory = ['taskService', function(taskService) {
			var requestJob = function(name, data) {
			};
			var getAvailableJobs = function() {
				return jobTemplates;
			};
		
			var theService = {
				getAvailableJobs: getAvailableJobs,
				requestJob: requestJob,
			};
			return theService;
		}];
		return {
			config: config,
			$get: serviceFactory,
		};
	}])